#include <stdio.h>
#include <stdlib.h>

//Node struct with value, and pointer to both childs
struct node {
    int value;
    struct node* leftChild;
    struct node* rightChild;
};



int main() {

    return 0;
}